<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <title>HackYourTemperature</title>

    <style>
    html,
    body {
        height: 100%;
        background-image: url('https://source.unsplash.com/1600x900/?weather');
        background-position: center center;
        background-size: cover;
    }

    body {
        display: flex;
        align-items: center;
        padding:0 40px;
        background-color: #f5f5f5;
    }
    .form-layout {
        border: 2px solid blue transparent;
        border-radius: 10px;
        width: 100%;
        max-width: 330px;
        padding: 35px;
        margin: auto;
    }
    .form-layout .form-control {
        position: relative;
        box-sizing: border-box;
        height: auto;
        padding: 10px;
        font-size: 16px;
    }
    .form-layout .form-control:focus {
         z-index: 2;
    }
    .form-layout input[type="text"] {
        margin-bottom: 10px;
        border-bottom-right-radius: 0;
        border-bottom-left-radius: 0;
    }
    .form-layout button{
         border-top-right-radius: 0;
        border-top-left-radius: 0;
    }
  
    </style>
</head>
<body class="text-center">
    <main class="form-layout">
        <form action="/weather" method="POST">
            <input type="text" name="cityName" class="bg-transparent form-control" placeholder="Enter a city name ">
            <Button type="submit" class="clickBtn w-100 btn btn-lg btn-primary text-capitalize ">Search</Button>
            <hr class="bg-transparent">
            <h2 class="pt-3 pb-3 text-uppercase text-success ">{{cityName}}</h2>
            <p class="text-white h4 ">{{weatherText}}</p>
        </form>
    </main>

</body>
</html>